import{Game,WORLD,WALLS,dist}from './engine.js';
const $=id=>document.getElementById(id),canvas=$('game'),ctx=canvas.getContext('2d'),game=new Game();
const keys=new Set(),input={x:0,y:0,attack:false,shield:false,interact:false};
let width=innerWidth,height=innerHeight,dpr=1,camera={x:0,y:0},zoom=1,last=0,frame=0,previousState='ready',toastUntil=0,assetsReady=false,mouseDown=false,joy={x:0,y:0},touchAttack=false,touchShield=false,soundOn=true,audio=null,prevKills=0,prevBlocks=0,prevHp=100,lastAttack=0;
const coarse=matchMedia('(pointer:coarse)').matches||navigator.maxTouchPoints>0;
const atlas=new Image();atlas.src='assets/characters.png';
atlas.onload=()=>{assetsReady=true;$('assetStatus').hidden=true;$('play').disabled=false;};
atlas.onerror=()=>{$('assetStatus').textContent='Не удалось загрузить героев. Обнови страницу.';$('play').disabled=true;};
$('play').disabled=true;
const sprites={warrior:{x:45,y:10,w:525,h:990},cowboy:{x:542,y:96,w:451,h:905},bandit:{x:1010,y:170,w:500,h:828}};
function resize(){width=innerWidth;height=innerHeight;dpr=Math.min(devicePixelRatio||1,2);canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);zoom=Math.max(.48,Math.min(1.2,width/1050,height/660));if(width<600&&height>width)zoom=.64;ctx.setTransform(dpr,0,0,dpr,0,0);}
addEventListener('resize',resize);resize();
function initSound(){if(!audio){try{audio=new(window.AudioContext||window.webkitAudioContext)()}catch{soundOn=false;}}if(audio?.state==='suspended')audio.resume();}
function tone(f,d=.07,type='triangle',vol=.055){if(!audio||!soundOn)return;const o=audio.createOscillator(),g=audio.createGain();o.type=type;o.frequency.setValueAtTime(f,audio.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(30,f*.5),audio.currentTime+d);g.gain.setValueAtTime(vol,audio.currentTime);g.gain.exponentialRampToValueAtTime(.001,audio.currentTime+d);o.connect(g);g.connect(audio.destination);o.start();o.stop(audio.currentTime+d);}
function clearInput(){keys.clear();mouseDown=false;touchShield=false;touchAttack=false;joy={x:0,y:0};$('knob').style.transform='';$('shieldTouch').classList.remove('held');$('attackTouch').classList.remove('held');}
function showToast(t){$('toast').textContent=t;$('toast').classList.add('show');toastUntil=performance.now()+4300;}
function formatTime(t){return Math.floor(t/60)+':'+String(Math.floor(t%60)).padStart(2,'0');}
function setPlayingUI(){const playing=game.state==='playing';$('hud').hidden=!playing;$('desktopHelp').hidden=!playing||coarse;$('touch').hidden=!playing||!coarse;$('overlay').hidden=playing;$('allyHud').hidden=!playing||!game.rescued;if(!playing){$('interact').hidden=true;$('toast').classList.remove('show');}}
function begin(){if(!assetsReady)return;initSound();game.begin();clearInput();camera={x:0,y:0};prevKills=prevBlocks=0;prevHp=100;previousState='playing';$('overlay').className='overlay';setPlayingUI();tone(380,.12);}
function pause(){if(game.state==='playing'){game.state='paused';clearInput();$('overlay').className='overlay paused';$('cast').hidden=true;$('menuTitle').innerHTML='ПЕРЕДЫШКА';$('menuSub').textContent='МИССИЯ ПРИОСТАНОВЛЕНА';$('menuCopy').textContent='Соберись с мыслями. Напарник ждёт.';$('play').textContent='ПРОДОЛЖИТЬ';$('restart').hidden=false;$('results').hidden=true;$('menuFoot').textContent='WASD: движение · F: удар · Пробел: щит';setPlayingUI();}else if(game.state==='paused'){game.state='playing';clearInput();setPlayingUI();}}
$('play').onclick=()=>{if(game.state==='paused')pause();else begin();};$('restart').onclick=begin;$('pause').onclick=pause;
$('sound').onclick=()=>{soundOn=!soundOn;initSound();$('sound').textContent=soundOn?'♪':'♪̸';$('sound').setAttribute('aria-label',soundOn?'Выключить звук':'Включить звук');};
$('interact').onclick=()=>{if(game.free())tone(700,.2);};
addEventListener('keydown',e=>{if(['Space','ArrowUp','ArrowDown','ArrowLeft','ArrowRight','KeyW','KeyA','KeyS','KeyD','KeyF','KeyE','Escape','KeyP'].includes(e.code))e.preventDefault();keys.add(e.code);if((e.code==='Escape'||e.code==='KeyP')&&!e.repeat)pause();if(e.code==='KeyE'&&!e.repeat)game.free();});
addEventListener('keyup',e=>keys.delete(e.code));
canvas.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'&&e.button===0){mouseDown=true;canvas.setPointerCapture(e.pointerId);initSound();}});canvas.addEventListener('pointerup',()=>mouseDown=false);canvas.addEventListener('pointercancel',()=>mouseDown=false);canvas.addEventListener('contextmenu',e=>e.preventDefault());
addEventListener('blur',()=>{clearInput();if(game.state==='playing')pause();});document.addEventListener('visibilitychange',()=>{if(document.hidden&&game.state==='playing')pause();});
const stick=$('stick');let stickId=null;
function stickMove(e){if(stickId!==e.pointerId)return;const r=stick.getBoundingClientRect(),dx=e.clientX-r.left-r.width/2,dy=e.clientY-r.top-r.height/2,d=Math.hypot(dx,dy),m=Math.min(38,d),a=Math.atan2(dy,dx);joy={x:Math.cos(a)*m/38,y:Math.sin(a)*m/38};$('knob').style.transform=`translate(${joy.x*32}px,${joy.y*32}px)`;}
stick.onpointerdown=e=>{stickId=e.pointerId;stick.setPointerCapture(e.pointerId);stickMove(e);initSound();};stick.onpointermove=stickMove;stick.onpointerup=stick.onpointercancel=e=>{if(e.pointerId===stickId){stickId=null;joy={x:0,y:0};$('knob').style.transform='';}};
function touchButton(id,set){const el=$(id);el.onpointerdown=e=>{el.setPointerCapture(e.pointerId);set(true);el.classList.add('held');initSound();};el.onpointerup=el.onpointercancel=()=>{set(false);el.classList.remove('held');};}
touchButton('attackTouch',v=>touchAttack=v);touchButton('shieldTouch',v=>touchShield=v);
const map=document.createElement('canvas');map.width=WORLD.w;map.height=WORLD.h;const m=map.getContext('2d');
let seed=917;function rnd(){seed=(seed*1664525+1013904223)>>>0;return seed/4294967296;}
function roundRect(c,x,y,w,h,r=5){c.beginPath();c.roundRect(x,y,w,h,r);}
function buildMap(){m.fillStyle='#e7ddc8';m.fillRect(0,0,WORLD.w,WORLD.h);
 m.strokeStyle='#c2b39755';m.lineWidth=1;for(let i=0;i<15000;i++){const x=rnd()*WORLD.w,y=rnd()*WORLD.h;m.globalAlpha=.13+rnd()*.22;m.beginPath();m.moveTo(x,y);m.lineTo(x+rnd()*8-4,y+rnd()*3);m.stroke();}m.globalAlpha=1;
 m.strokeStyle='#d9caae';m.lineWidth=190;m.lineCap='round';m.beginPath();m.moveTo(130,800);m.lineTo(520,800);m.lineTo(790,770);m.lineTo(1230,550);m.lineTo(1500,530);m.lineTo(2110,460);m.stroke();
 m.setLineDash([8,12]);m.strokeStyle='#b4a38155';m.lineWidth=2;m.beginPath();m.moveTo(140,865);m.lineTo(480,865);m.lineTo(830,825);m.lineTo(1250,605);m.lineTo(1530,590);m.lineTo(2100,530);m.stroke();m.setLineDash([]);
 for(let i=0;i<220;i++){const x=rnd()*WORLD.w,y=rnd()*WORLD.h;m.fillStyle='#756b5140';m.beginPath();m.ellipse(x,y,2+rnd()*5,1+rnd()*2,rnd()*3,0,Math.PI*2);m.fill();}
 for(const w of WALLS){m.fillStyle='#453c3230';m.fillRect(w.x+8,w.y+12,w.w,w.h);m.fillStyle=w.w>70&&w.h>50?'#b7a88c':'#bfb29b';m.fillRect(w.x,w.y,w.w,w.h);m.strokeStyle='#5a574c';m.lineWidth=2;m.strokeRect(w.x,w.y,w.w,w.h);m.strokeStyle='#655b4655';m.lineWidth=1;
 if(w.w>70&&w.h>50){m.strokeRect(w.x+5,w.y+5,w.w-10,w.h-10);m.beginPath();m.moveTo(w.x+5,w.y+5);m.lineTo(w.x+w.w-5,w.y+w.h-5);m.moveTo(w.x+w.w-5,w.y+5);m.lineTo(w.x+5,w.y+w.h-5);m.stroke();}
 else {for(let x=w.x+10;x<w.x+w.w;x+=42){m.beginPath();m.moveTo(x,w.y);m.lineTo(x+5,w.y+w.h);m.stroke();}for(let y=w.y+18;y<w.y+w.h;y+=36){m.beginPath();m.moveTo(w.x,y);m.lineTo(w.x+w.w,y);m.stroke();}}
 m.strokeStyle='#eee4ceaa';m.beginPath();m.moveTo(w.x+2,w.y+2);m.lineTo(w.x+w.w-2,w.y+2);m.stroke();}
 function label(t,x,y,size=23){m.save();m.translate(x,y);m.rotate(-.03);m.fillStyle='#73634666';m.font=`bold ${size}px Georgia`;m.fillText(t,0,0);m.restore();}
 label('ПОДСТУПЫ',205,265,30);label('ВНЕШНИЙ ДВОР',835,255,28);label('ФОРТ ЖЁЛТОГО ШАРФА',1610,210,25);label('ПУТЬ НАЗАД',120,1190,21);
 m.strokeStyle='#378178';m.lineWidth=4;m.setLineDash([10,8]);m.strokeRect(100,690,125,220);m.setLineDash([]);m.fillStyle='#388079';m.font='bold 18px Arial';m.fillText('ВЫХОД',128,939);
}
buildMap();
function drawSprite(e,kind,scale=1){const r=sprites[kind],h=(kind==='warrior'?108:94)*scale,w=r.w/r.h*h;const bob=e.moving?Math.sin(game.time*15)*2.7:Math.sin(game.time*2)*.6;
 ctx.save();ctx.translate(e.x,e.y);ctx.fillStyle='#2c292926';ctx.beginPath();ctx.ellipse(0,6,w*.34,9*scale,0,0,Math.PI*2);ctx.fill();if(e.inv>0&&Math.floor(e.inv*17)%2===0)ctx.globalAlpha=.55;
 const flip=Math.cos(e.angle)<-.1;ctx.translate(0,-h*.43+bob);if(flip)ctx.scale(-1,1);if(e.moving)ctx.rotate(Math.sin(game.time*15)*.028);ctx.globalCompositeOperation='multiply';
 if(assetsReady){if(kind==='warrior'){ctx.save();ctx.beginPath();ctx.moveTo(-w/2,-h/2);ctx.lineTo(-w/2+490/r.w*w,-h/2);ctx.lineTo(-w/2+490/r.w*w,-h/2+375/r.h*h);ctx.lineTo(w/2,-h/2+375/r.h*h);ctx.lineTo(w/2,h/2);ctx.lineTo(-w/2,h/2);ctx.closePath();ctx.clip();ctx.drawImage(atlas,r.x,r.y,r.w,r.h,-w/2,-h/2,w,h);ctx.restore();}else ctx.drawImage(atlas,r.x,r.y,r.w,r.h,-w/2,-h/2,w,h);}ctx.restore();
 if(e.hp<e.maxHp&&e.hp>0){ctx.fillStyle='#34353844';ctx.fillRect(e.x-23,e.y-113*scale,46,5);ctx.fillStyle=kind==='cowboy'?'#2d8a9d':'#b62e34';ctx.fillRect(e.x-23,e.y-113*scale,46*e.hp/e.maxHp,5);}
}
function marker(x,y,color,label){ctx.save();ctx.translate(x,y);ctx.strokeStyle=color;ctx.lineWidth=2;ctx.setLineDash([5,7]);ctx.beginPath();ctx.ellipse(0,0,48,22,0,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle=color;ctx.font='bold 13px Arial';ctx.textAlign='center';ctx.fillText(label,0,40);ctx.restore();}
function arrowTo(target,label,color){const sx=(target.x-camera.x)*zoom,sy=(target.y-camera.y-50)*zoom;if(sx>65&&sx<width-65&&sy>145&&sy<height-100)return;const cx=width/2,cy=height/2,a=Math.atan2(sy-cy,sx-cx),rx=width/2-65,ry=height/2-125,t=Math.min(rx/Math.max(.001,Math.abs(Math.cos(a))),Math.max(45,ry)/Math.max(.001,Math.abs(Math.sin(a)))),x=cx+Math.cos(a)*t,y=cy+Math.sin(a)*t;ctx.save();ctx.translate(x,y);ctx.rotate(a);ctx.fillStyle=color;ctx.beginPath();ctx.moveTo(16,0);ctx.lineTo(-9,-10);ctx.lineTo(-5,0);ctx.lineTo(-9,10);ctx.closePath();ctx.fill();ctx.restore();ctx.fillStyle='#312f2d';ctx.font='bold 12px Arial';ctx.textAlign='center';ctx.fillText(label,x,y+26);}
function minimap(){if(width<760)return;const w=155,h=96,x=width-w-24,y=height-h-80;ctx.fillStyle='#f4ecdddb';roundRect(ctx,x-5,y-5,w+10,h+10);ctx.fill();ctx.strokeStyle='#8c7d6255';ctx.stroke();const sx=w/WORLD.w,sy=h/WORLD.h;ctx.fillStyle='#82776390';for(const r of WALLS)ctx.fillRect(x+r.x*sx,y+r.y*sy,Math.max(2,r.w*sx),Math.max(2,r.h*sy));function dot(e,c,s=3){ctx.fillStyle=c;ctx.beginPath();ctx.arc(x+e.x*sx,y+e.y*sy,s,0,Math.PI*2);ctx.fill();}dot(game.exit,'#398a80',4);dot(game.ally,'#348fa3');dot(game.player,'#bf3138',4);ctx.strokeStyle='#ac3037';ctx.strokeRect(x+camera.x*sx,y+camera.y*sy,Math.min(w,width/zoom*sx),Math.min(h,height/zoom*sy));}
function draw(){ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,width,height);ctx.fillStyle='#e7ddc8';ctx.fillRect(0,0,width,height);
 const focus=game.state==='ready'?{x:1100,y:700}:game.player;const tx=Math.max(0,Math.min(WORLD.w-width/zoom,focus.x-width/zoom*.47)),ty=Math.max(0,Math.min(WORLD.h-height/zoom,focus.y-height/zoom*.56));camera.x+=(tx-camera.x)*.12;camera.y+=(ty-camera.y)*.12;
 ctx.save();ctx.scale(zoom,zoom);ctx.translate(-camera.x,-camera.y);ctx.drawImage(map,0,0);
 for(const item of game.pickups){if(item.used)continue;ctx.save();ctx.translate(item.x,item.y);ctx.fillStyle='#fcf7e8';ctx.strokeStyle='#6d7863';ctx.lineWidth=2;roundRect(ctx,-17,-13,34,27,4);ctx.fill();ctx.stroke();ctx.fillStyle='#ae3c40';ctx.fillRect(-3,-9,6,18);ctx.fillRect(-10,-3,20,6);ctx.restore();}
 if(!game.rescued){marker(game.ally.x,game.ally.y,'#2c7f92','НАПАРНИК');ctx.strokeStyle='#645b4ca0';ctx.lineWidth=3;for(let i=0;i<5;i++){ctx.beginPath();ctx.moveTo(2020+i*40,335);ctx.lineTo(2020+i*40,565);ctx.stroke();}}
 else {marker(game.exit.x,game.exit.y,'#3b8774','СЮДА, ВМЕСТЕ!');}
 const entities=[...game.enemies.filter(e=>e.hp>0).map(e=>({e,kind:'bandit'})),{e:game.ally,kind:'cowboy'},{e:game.player,kind:'warrior'}].sort((a,b)=>a.e.y-b.e.y);
 for(const{e,kind}of entities){if(e.x<camera.x-100||e.x>camera.x+width/zoom+100||e.y<camera.y-100||e.y>camera.y+height/zoom+120)continue;
 if(e.type&&e.wind>0){ctx.strokeStyle='#b62e3455';ctx.setLineDash([6,8]);ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(e.x,e.y-10);ctx.lineTo(e.x+Math.cos(e.angle)*250,e.y+Math.sin(e.angle)*250-10);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#bc3137';ctx.font='bold 24px Arial';ctx.textAlign='center';ctx.fillText('!',e.x,e.y-105);}
 drawSprite(e,kind,e.type==='boss'?1.22:e.type==='melee'?.9:1);
 if(e.type==='boss'){ctx.fillStyle='#463d2c';ctx.font='bold 12px Arial';ctx.textAlign='center';ctx.fillText('ЖЁЛТЫЙ ШАРФ',e.x,e.y-151);}
 }
 const p=game.player;if(p.shield){ctx.save();ctx.translate(p.x,p.y-15);ctx.strokeStyle='#e7bc4e';ctx.lineWidth=7;ctx.shadowColor='#efc865';ctx.shadowBlur=8;ctx.beginPath();ctx.arc(0,0,44,p.angle-1.2,p.angle+1.2);ctx.stroke();ctx.restore();}
 for(const b of game.bullets){ctx.strokeStyle=b.friendly?'#2d92ad':'#ba6932';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(b.x-b.dx*15,b.y-b.dy*15-15);ctx.lineTo(b.x,b.y-15);ctx.stroke();}
 for(const f of game.effects){ctx.save();ctx.globalAlpha=f.t/f.max;if(f.swing){ctx.strokeStyle='#a52c35';ctx.lineWidth=4;ctx.beginPath();ctx.arc(f.x,f.y-20,80,f.angle-.9,f.angle+.9);ctx.stroke();}else if(f.flash){ctx.fillStyle=f.color;ctx.beginPath();ctx.arc(f.x,f.y,8,0,Math.PI*2);ctx.fill();}else{ctx.fillStyle=f.color;ctx.strokeStyle='#fff5df';ctx.lineWidth=3;ctx.font='bold 18px Arial';ctx.textAlign='center';const y=f.y-(1-f.t/f.max)*30;ctx.strokeText(f.text,f.x,y);ctx.fillText(f.text,f.x,y);}ctx.restore();}
 ctx.restore();
 if(game.state==='playing'){arrowTo(game.rescued?game.exit:game.ally,game.rescued?'К ВОРОТАМ':'КОВБОЙ',game.rescued?'#397e6c':'#b62e34');minimap();if(game.player.hp<30){const grad=ctx.createRadialGradient(width/2,height/2,height*.25,width/2,height/2,Math.max(width,height)*.65);grad.addColorStop(0,'#a9232700');grad.addColorStop(1,'#a9232735');ctx.fillStyle=grad;ctx.fillRect(0,0,width,height);}}
}
function endScreen(){const win=game.state==='won';clearInput();$('overlay').className='overlay end';$('cast').hidden=true;$('menuTitle').innerHTML=win?'СВОИХ<br><em>НЕ БРОСАЕМ</em>':'ЕЩЁ<br><em>ПОПЫТКА?</em>';$('menuSub').textContent=win?'МИССИЯ ВЫПОЛНЕНА':'МИССИЯ НЕ ЗАВЕРШЕНА';$('menuCopy').textContent=win?'Вы оба добрались до ворот. Хорошая работа, командир.':game.reason;$('results').hidden=false;$('results').innerHTML=`<div><b>${formatTime(game.time)}</b><span>время</span></div><div><b>${game.kills}</b><span>побеждено</span></div><div><b>${game.blocks}</b><span>блоков щитом</span></div>`;$('play').textContent=win?'СЫГРАТЬ ЕЩЁ':'ПОПРОБОВАТЬ СНОВА';$('restart').hidden=true;$('menuFoot').textContent=win?'Ты прикрывал. Он доверял. Вы справились.':'Укрытие, щит, удар. И ещё один шанс.';setPlayingUI();tone(win?660:140,.3,'triangle');}
function updateHUD(){const p=game.player;$('hpText').textContent=p.hp;$('hpBar').style.width=p.hp+'%';$('energyBar').style.width=p.energy+'%';$('allyBar').style.width=game.ally.hp+'%';$('allyText').textContent=game.ally.hp;$('clock').textContent=formatTime(game.time);$('chapter').textContent=game.rescued?'02 / ВОЗВРАЩЕНИЕ':'01 / ПРОНИКНОВЕНИЕ';$('goal').textContent=game.rescued?'Вернитесь к воротам вместе':'Освободи ковбоя в форте';$('allyHud').hidden=!game.rescued||game.state!=='playing';$('interact').hidden=game.rescued||game.state!=='playing'||dist(p,game.ally)>115;}
function loop(t){const dt=Math.min((t-last)/1000,.05)||0;last=t;input.x=(keys.has('KeyD')||keys.has('ArrowRight')?1:0)-(keys.has('KeyA')||keys.has('ArrowLeft')?1:0)+joy.x;input.y=(keys.has('KeyS')||keys.has('ArrowDown')?1:0)-(keys.has('KeyW')||keys.has('ArrowUp')?1:0)+joy.y;input.attack=mouseDown||keys.has('KeyF')||touchAttack;input.shield=keys.has('Space')||touchShield;game.update(dt,input);
 if(game.state==='playing'){if(game.player.swing>lastAttack)tone(190,.065,'sawtooth',.025);lastAttack=game.player.swing;if(game.blocks>prevBlocks)tone(850,.075,'square',.025);if(game.kills>prevKills)tone(360,.11);if(game.player.hp<prevHp)tone(85,.15,'sawtooth',.055);prevBlocks=game.blocks;prevKills=game.kills;prevHp=game.player.hp;while(game.events.length)showToast(game.events.shift());if(t>toastUntil)$('toast').classList.remove('show');if(frame%5===0)updateHUD();}
 if(game.state!==previousState&&(game.state==='won'||game.state==='lost'))endScreen();previousState=game.state;draw();frame++;requestAnimationFrame(loop);}
requestAnimationFrame(loop);
// Read-only bridge for inspecting a running mission; never changes combat state.
window.redShield={status:()=>game.snapshot()};
if(document.modelContext?.registerTool){try{void Promise.resolve(document.modelContext.registerTool({name:'mission_status',description:'Read the current Red Shield mission status, health and objective.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:async(input)=>{if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw new Error('Параметры не требуются');return game.snapshot();}})).catch(()=>{});}catch{}}
